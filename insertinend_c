#include <stdio.h>

int main() {
    int arr[10] = {1, 2, 3, 4, 5}; 
    int size = 5; 
    int newElement = 10; 

    arr[size] = newElement;
    size++; 

    printf("Updated array: ");
    for (int i = 0; i < size; i++) {
        printf("%d ", arr[i]);
    }
    return 0;
}
